console.log("Hi there!");

const botName = "teacherBot";

const greeting = `My name is ${botName}.`;
console.log(greeting);

const subject = "JavaScript";
const topic = "strings";
console.log("");

const sentence = `Today, you will learn about ${topic} in ${subject}.`;
console.log(sentence);

let strLengthIntro = `Here is an example of using the length property on the word ${subject}.`;
console.log(strLengthIntro);

let length = console.log(subject.length);

let step8 = `Here is an example of using the length property on the word ${topic}.`;
console.log(step8);
console.log(step8.length);
